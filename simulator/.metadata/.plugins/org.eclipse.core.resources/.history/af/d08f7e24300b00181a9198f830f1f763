
#include "utility.h"

void waitAndDeletePackets(omnetpp::simtime_t time)
{
    omnetpp::cQueue queue;
    waitAndEnqueue(time,&queue);
    //TODO: is this efficient? And most important, why can't they use std::list
    while(!queue.empty()) delete queue.pop();

}
